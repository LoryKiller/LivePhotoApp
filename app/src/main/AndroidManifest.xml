<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.livephoto.app">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_live_photo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_live_photo"
        android:supportsRtl="true"
        android:theme="@style/Theme.LivePhotoApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LivePhotoViewerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.LivePhotoApp.Fullscreen" />

    </application>

</manifest>
